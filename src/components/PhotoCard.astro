---
import type { PhotoItem } from "@/data/photos";

type Props = { photo: PhotoItem };
const { photo } = Astro.props;
---

<button
  class="photo-card"
  type="button"
  data-photo-src={photo.src}
  aria-label="Open photo"
>
  <img src={photo.src} alt="" loading="lazy" />
</button>
